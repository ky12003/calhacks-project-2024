import { Button } from '@mantine/core' // Import Button component from Mantine

export default function AddTaskButton() { // Create AddTaskButton component as a submit button
  return <Button type="submit">Add Task</Button>
}