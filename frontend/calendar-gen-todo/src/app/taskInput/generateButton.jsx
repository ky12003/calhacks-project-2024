import { Button } from '@mantine/core' // Import Button component from Mantine.

export default function GenerateButton() { // Renders an outlined button component.
  return <Button type="button" variant="outline" className="ml-2">Generate</Button>
}